{
  "name": "worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply scholar-tracker --local",
    "db:migrate:remote": "wrangler d1 migrations apply scholar-tracker --remote"
  },
  "dependencies": {
    "hono": "^4.6.16"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250810.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.27.0"
  }
}
